import 'package:flutter/material.dart';

class VendaOptionsButton extends StatelessWidget {
  const VendaOptionsButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
